<div class="quiz-container">
  <div class="header">
    <h1>QUIZZ</h1>
    <div class="user-icon">👤</div>
  </div>

  <div class="quiz-content">
    <%= image_tag "quiz_image.png", alt: "Quiz Time", class: "quiz-image" %>

    <h2><%= @question.content %></h2>
    <p>Une seule réponse à cocher</p>

    <%= form_with url: submit_quizzes_path, method: :post, local: true do |f| %>
      <% @answers.each do |answer| %>
        <div class="answer-option">
          <%= f.radio_button :answer_id, answer.id, id: "answer_#{answer.id}" %>
          <%= f.label "answer_#{answer.id}", answer.content %>
        </div>
      <% end %>

      <%= f.submit "Soumettre", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
