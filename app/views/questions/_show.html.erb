<div class='container'>
  <div class="header text-center">
    <h1>Ton programme personnalisé</h1>
    <p>Voici une liste de contenus adaptés à tes <span class="highlight">craintes</span> :</p>
  </div>

  <% if @documents.any? %>
    <div class="documents-list">
      <% @documents.each do |document| %>
        <div class="document-item">
          <h3><%= document.title %></h3>
          <p><%= document.content %></p>
          <% if document.kind == "video" %>
            <video controls>
              <source src="<%= document.url %>" type="video/mp4">
              Votre navigateur ne supporte pas la lecture des vidéos.
            </video>
          <% elsif document.kind == "article" %>
            <a href="<%= document.url %>" target="_blank" class="btn btn-link">Lire l'article</a>
          <% end %>
        </div>
      <% end %>
    </div>
  <% else %>
    <p>Aucun document n'est disponible pour vos peurs sélectionnées.</p>
  <% end %>
</div>
